# Frontend (React + Vite) — variáveis de ambiente necessárias.
# Copie este arquivo para ".env" na mesma pasta e preencha com os valores reais.

# URL base do backend (Express)
VITE_API_URL=http://192.168.15.108:5000
